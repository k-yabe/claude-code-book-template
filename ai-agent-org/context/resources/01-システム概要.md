# AI Agent Organization — システム概要

> AIエージェントを「会社の組織」のように動かし、あなたの代わりに仕事を自動でこなすシステムです。

---

## 全体の仕組み

```
あなた
  │
  │  ① inbox にメモを入れる
  ▼
┌─────────────────────────────────────────────────────┐
│                  AI Agent Organization               │
│                                                     │
│  inbox/          tasks/          context/           │
│  ┌──────┐  →   ┌────────┐  ←  ┌───────────────┐   │
│  │メモ.md│      │tasks.json│     │あなたの情報   │   │
│  └──────┘      └────────┘     │（philosophy,  │   │
│                    ↓          │ identity...） │   │
│              ┌──────────┐     └───────────────┘   │
│              │ AI エージェント │                        │
│              │ が自動実行 │                        │
│              └──────────┘                        │
│                    ↓                               │
│              context/projects/                     │
│              ┌──────────────┐                      │
│              │ 成果物.md    │                      │
│              └──────────────┘                      │
└─────────────────────────────────────────────────────┘
  │
  │  ② 結果を GitHub で確認 or タスクボードで管理
  ▼
あなた（朝会レポートで確認）
```

---

## 3つの主要フロー

### フロー① タスクの自動生成（毎時）

```
あなたがメモを inbox/ に入れる
         │
         ▼
  task-dispatcher.js
  （1時間ごとに自動実行）
         │
    内容を分析して
  ・優先度を判定（P0〜P3）
  ・担当エージェントを決定
  ・組織を分類
         │
         ▼
  tasks/tasks.json に追加
         │
         ▼
  GitHub に自動 push
```

**例：** `inbox/` に「動画の台本を書いて」というメモを入れると → `content-director` エージェントに P2 タスクとして自動登録される

---

### フロー② AIによるタスク自動実行（毎時）

```
tasks.json の new-assigned タスクを確認
         │
         ▼
  task-executor.js
  （1時間ごとに自動実行）
         │
    担当エージェントに合わせた
    Claude モデルで処理
    ・調査・リサーチ系 → Web検索も使用
    ・戦略判断 → Sonnet（高性能）
    ・その他 → Haiku（高速・低コスト）
         │
         ▼
  成果物を context/projects/ に保存
  tasks.json のステータスを done に更新
         │
         ▼
  GitHub に自動 push
```

---

### フロー③ 毎朝の自動レポート（7:00）

```
  morning-standup.js
  （毎朝7時に自動実行）
         │
    tasks.json を読んで集計
    ・昨夜完了したタスク
    ・進行中・ブロック中
    ・チーム別状況
         │
         ▼
  context/context/ai-handoffs/standup-YYYY-MM-DD.md
         │
         ▼
  GitHub に自動 push
```

---

## 自動実行スケジュール

```
 02:00  ━━━━━━━━━━━━━━━━━━━━━━━━  overnight-qa.js
        データ整合性チェック・QAレポート生成

 07:00  ━━━━━━━━━━━━━━━━━━━━━━━━  morning-standup.js
        朝会レポート生成

 毎時   ━━━━━━━━━━━━━━━━━━━━━━━━  task-dispatcher.js
        inbox 監視 → タスク自動生成

 毎時   ━━━━━━━━━━━━━━━━━━━━━━━━  task-executor.js
        new-assigned タスクを AI が自動実行
```

---

## ディレクトリ構造

```
ai-agent-org/
│
├── context/                    ← 知識ベース（全エージェントが参照）
│   ├── inbox/                  ← ここにメモを入れる（投入口）
│   ├── projects/               ← AIが作った成果物
│   ├── ideas/                  ← アイデア置き場
│   ├── resources/              ← このドキュメント
│   └── context/                ← あなた自身の情報
│       ├── professional-identity.md  ← 職歴・スキル
│       ├── philosophy.md             ← 仕事の哲学・価値観
│       ├── visual-design.md          ← デザインシステム
│       ├── technical-setup.md        ← 技術スタック
│       └── ai-handoffs/              ← AI間の引き継ぎ記録
│
├── agents/                     ← エージェントの役割定義
│   ├── engineering/            ← 開発チーム
│   ├── content/                ← コンテンツチーム
│   ├── business/               ← ビジネスチーム
│   └── infrastructure/         ← インフラチーム
│
├── tasks/
│   └── tasks.json              ← タスクデータベース（全タスクはここ）
│
├── apps/
│   └── task-board/             ← ブラウザで見るカンバンボード
│       └── index.html
│
└── scripts/                    ← 自動化スクリプト
    ├── task-dispatcher.js      ← inbox → タスク生成
    ├── task-executor.js        ← AI タスク自動実行
    ├── morning-standup.js      ← 朝会レポート
    ├── overnight-qa.js         ← 夜間品質チェック
    └── mac-setup/              ← Mac 自動実行設定
```

---

## タスクのステータス遷移

```
inbox に                  AIが
メモを入れる              自動実行
    │                       │
    ▼                       ▼
new-assigned  →  in-progress  →  done
（未着手）       （実行中）       （完了）
    │                │
    │           判断が必要な
    │           ケースのみ
    │                ▼
    └──────────  blocked
                 （要確認）
                      │
                 あなたがコメントを入れて
                 「🔁 再実行」ボタンを押す
                      │
                      ▼
                 new-assigned に戻る
```

---

## 優先度の意味

| 優先度 | 意味 | キーワード |
|--------|------|-----------|
| P0 🔴 | 緊急・今すぐ対応 | 「緊急」「至急」「クリティカル」 |
| P1 🟠 | 重要・今日中に | 「重要」「高優先」 |
| P2 🟡 | 通常（デフォルト） | — |
| P3 🔵 | 低優先・いつか | 「低優先」「いつか」 |
