# [S098] ROI / ROAS Simulator — 初見向け情報整理＋用語平易化

**ステータス**: 完了
**作成日**: 2026-06-21
**完了日**: 2026-06-21

---

## 目的

「ごちゃごちゃして分かりにくい・初見の人が使えない」「『経済性』という言葉が分からない」という指摘に対応し、初めて触る人でも迷わず使えるように情報量と用語を整理する。

## スコープ
- [x] 用語平易化：ユーザー向けの「経済性」を「前提値（成約率・客単価など）」に統一
- [x] 段階的開示：損益分岐バー＋ファネル内訳を「📊 計算の内訳を見る」折りたたみに格納
- [x] 初見が最初に見るのは「判定＋ひとこと＋3指標」だけに
- [x] 印刷整合：内訳は PDF では常に展開／左カラム再構成に伴う print 非表示セレクタ修正
- [x] 回帰テストの文言チェック更新（143 維持）／WHAT'S NEW・onboarding・design.md 更新

## 完了条件
- [x] ユーザー向け文言に「経済性」が出ない
- [x] 結果カードの既定表示が判定＋ひとこと＋3指標に絞られる（内訳は折りたたみ）
- [x] PDF 出力には内訳が展開して載る／入力カードは印刷に出ない
- [x] `npm run test:roi` 全 pass（143）

## 影響ファイル
| ファイル | 変更種別 |
|---------|---------|
| `apps/roi-roas-simulator/index.html` | 変更 |
| `tests/roi-roas-simulator.e2e.js` | 変更（basis-bar 文言を「前提値」に） |
| `index.html`（WHAT'S NEW） | 変更 |
| `docs/design.md`（S098） | 変更 |
| `steering/S098-*.md` | 新規 |

## 参照
- ユーザー報告（本セッション）: 「ごちゃごちゃしてわかり難い。これ初見の人使えないでしょ」「経済性って言葉よくわからないな。正しい日本語に」
- `steering/S082`〜`S097`

## 作業ログ
- 2026-06-21: ユーザー向け「経済性」を「前提値」に全置換。結果カードの損益分岐バー＋ファネルを `result-breakdown` の折りたたみに格納し、初見の既定表示を判定＋ひとこと＋3指標に集約。印刷では内訳を常時展開、print の入力非表示を `.sim-col-left` に修正。回帰テスト文言更新で 143 全 pass。実機目視は Chromium 取得不可のため jsdom 構造検証＋ユーザー提供スクショ＋Vercelデプロイ後確認。
