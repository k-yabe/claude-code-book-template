# [S052] ワイヤーフレームメーカーバグ修正2件

**ステータス**: 完了
**作成日**: 2026-04-05
**完了日**: 2026-04-05

---

## 目的

Wireframe Makerを実際に操作した際に発見されたクラッシュバグ2件を修正する。

---

## スコープ

### やること
- [x] Bug1: `showUserFlow()` がクラッシュする — `p.links` が `{}` (オブジェクト) で `forEach` 不可
- [x] Bug2: ナビバーの「履歴」ボタンが `HistoryManager.togglePanel()` を呼ぶが存在しない

### やらないこと（スコープ外）
- 新機能追加
- UI/デザインの変更

---

## 完了条件

- [x] ユーザーフロー表示（🔀ボタン）がクラッシュせずに表示される
- [x] ナビバーの「履歴」ボタンが正常に動作する（パネルの開閉トグル）

---

## 影響ファイル

| ファイル | 変更種別 |
|---------|---------|
| `apps/wireframe-maker/index.html` | 変更 |

---

## 参照

- `steering/S047-ワイヤーフレームメーカー致命的バグ修正5件.md` — 前回のバグ修正

---

## 作業ログ

### Bug1: showUserFlow の p.links 型不整合
- 行6062: `links: p.links || []` — `p.links` が `{}` の場合 truthy なので `[]` にフォールバックしない
- 修正: `Array.isArray(p.links) ? p.links : []` に変更

### Bug2: HistoryManager.togglePanel 未定義
- 行743: `onclick="HistoryManager.togglePanel()"` を呼ぶが、HistoryManager には `openPanel` と `closePanel` のみ
- 修正: `togglePanel` メソッドを追加するか、onclick を修正
