# [S050] ワイヤーフレームメーカー致命的バグ4件修正（未定義関数・AIリライト・JSONインポート・SVGコピー）

**ステータス**: 完了 <!-- 未着手 | 進行中 | レビュー中 | 完了 -->
**作成日**: 2026-04-04
**完了日**: 2026-04-04

---

## 目的

UXオーディットで発見された致命的バグ4件を修正し、コラボ同期・Figmaインポート・AIリライト・JSONインポート・SVGコピーの各機能を正常に動作させる。

---

## スコープ

### やること
- [x] `renderSections()` → `renderSectionList()` / `requestPreviewRender()` → `renderPreview()` に修正（コラボ同期・コンポーネント挿入・Figmaインポート）
- [x] `cloneAIRewrite()` で `frame` 変数を宣言してクラッシュ修正
- [x] エクスポートタブ「JSON読込」ボタンが動作するよう修正（`proj-import-file` 要素の参照修正）
- [x] `copySVG()` のフィードバック表示を `textContent` から安全な方法に変更

### やらないこと（スコープ外）
- ツールバー整理（P1: 将来の作業単位）
- アクセシビリティ改善（P2: 将来の作業単位）

---

## 完了条件

- [x] `renderSections()` / `requestPreviewRender()` の呼び出しが全てエラーなく動作する
- [x] クローンモードでAIリライトがクラッシュせず実行される
- [x] エクスポートタブからJSONインポートが正常に動作する
- [x] SVGコピー後にエクスポートカードのレイアウトが崩れない

---

## 影響ファイル

| ファイル | 変更種別 |
|---------|---------|
| `apps/wireframe-maker/index.html` | 変更 |

---

## 参照

- `docs/design.md` — Wireframe Maker セクション
- `steering/S048-ワイヤーフレームメーカーUX致命的バグ4件修正.md` — 前回のUXバグ修正

---

## 作業ログ

<!-- 実装中に判明したことや決定事項をここに記録する -->
- UXオーディットで P0（致命的）として発見された4件
